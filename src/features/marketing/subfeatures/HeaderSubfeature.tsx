import React from 'react';

import {Header, Footer} from '../components';

export const HeaderSubfeature: React.FC = () => {
  return <Header />;
};

export const FooterSubfeature: React.FC = () => {
  return <Footer />;
};
